class Colors {
	readonly BACKGROUND = 'var(--background-color)';
	readonly PRIMARY = 'var(--primary-color)';
	readonly secondary = 'var(--secondary-color)';
	readonly TEXT = 'var(--text-color)';
	readonly BORDER = 'var(--border-color)';
	readonly ACCENT = 'var(--accent-color)';
	readonly DANGER = 'var(--danger-color)';
}

export const COLORS = new Colors();
