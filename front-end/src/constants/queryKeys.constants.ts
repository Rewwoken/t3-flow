class QueryKeys {
	readonly USER = ['user'];
	readonly TIMER_SETTINGS = ['timer', 'settings'];

	readonly MUTATE_AUTH = ['auth'];
	readonly MUTATE_SETTINGS = ['update', 'settings'];
}

export const QUERY_KEYS = new QueryKeys();
